# Memory Log - 2026-04-19

## 2026-04-19 18:35 UTC

### Service Role Key Reversion Decision
- User requested reverting `mood_log_insertion_block.js` and `heartbeat_sent_insertion_block.js` back to `SUPABASE_ANON_KEY` for now.
- Reason: Service role key needs to be retrieved from Supabase dashboard (Project Settings > API > service_role key) and added as a proper Railway environment variable externally.
- Action: Revert not yet applied; files still contain mixed references (variable declarations changed but headers still using `SUPABASE_SERVICE_ROLE_KEY`).
- Next step: After external key is set, swap key reference back.

### Deduplication Implementation
- Applied 24-hour deduplication window for mood_log insertions.
- Applied 25-minute deduplication window for heartbeat_sent insertions.
- Both insertion blocks now include existence checks before inserting duplicate records.

### Webhook Listener Integration
- Webhook listener (`/data/workspace/aof/webhook_listener.js`) running on `127.0.0.1:18792`.
- Signature verification supports both hex and base64, handles Supabase test webhooks.
- Integration with process manager via `start_cron.js` and `aof/cron.js` ensures restart on container reboots.

### Heartbeat Schedule Active
- Weekend schedule in effect (Sunday, April 19th).
- Morning Briefing moved to 12:00 PM Eastern (16:00 UTC).
- Mood check-in window: 12:00 PM–8:00 PM Eastern (16:00–00:00 UTC).
- All heartbeat checks returning clean (no open tasks, no NULL embeddings, mood logs present).