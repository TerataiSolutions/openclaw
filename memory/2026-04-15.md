# 2026‑04‑15

## System fixes (2026‑04‑14 UTC)

- **Duplicate cron manager resolved** – Removed restart loop, improved lock‑file validation, added zombie detection. Cron manager now runs as single process (PID 3524). Morning briefing scheduled for 8:15 AM Eastern.
- **NULL embedding issues fixed** – Updated `memory_integrity_check.js` to ignore NULL embeddings for importance < 7, changed protocol to always generate embeddings for all memories.
- **Morning briefing failure fixed** – Cron manager starts independently, lock‑file prevents duplicates.

## Protocol updates

- **Task‑aware context priming** – AGENTS.md routing table: `Log campaign:` → `campaign_metric` memories, `Log my activity:` → `personal_performance`, `Call review:` → `call_review`, `What do we know about` → `client_intel`. All other messages use standard semantic search.
- **Brevity principle added to SOUL.md** – “I default to the shortest response that fully answers the question. I expand only when depth is explicitly requested or when brevity would sacrifice necessary context. Verbosity is waste.”

## Follow‑up resolution

- **CPU temperature diagnosis** – Resolved independently. Resolution memory linked to original.
- **Voicemail scripts refinement** – Resolved independently. Resolution memory linked to original.

## Logging policy

- **Structured JSON logging** enabled for system scripts (`memory_integrity_check.js`, `semantic_search_selftest.js`, `memory_backup.js`, `auto_recover.js`). Proactive outreach scripts remain natural‑language prose.

## Commit
- GitHub: `9ce99b2` (closing follow‑up memories).