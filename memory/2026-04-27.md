# Memory Dump — 2026-04-27

## Architecture Cleanup (File Relocation)
- **`message_queue.js`** moved from root to `cron/message_queue.js`. All require paths updated in: `cron/message_bridge.js`, `cron/index.js`. Queue file path pinned to absolute `/data/.openclaw/workspace/message_queue.jsonl`.
- **`capability_monitor.js`** moved from root to `cron/capability_monitor.js`. All require paths updated in: `utils.js`, `cron/message_bridge.js`, `cron/dependency_healthcheck.js`.
- **Root `.js` count**: reduced from 21→19 across the two moves. Archive holds the remaining 72.

## Circular Dependency Resolution
- Confirmed the previously flagged circular risk (`message_queue.js` ↔ `message_bridge.js`, `capability_monitor.js` ↔ `utils.js`) is resolved. All four modules load without errors in a single require cycle: `utils.js` → `cron/capability_monitor.js` → `cron/message_bridge.js` → `cron/message_queue.js` → `utils.js` — no loop, single direction per edge.

## Dependency Healthcheck Cooldown
- `dependency_healthcheck.js` patched with cooldown system: suppresses alerts for same service within 30 minutes. Verified: first failure alerts, same service 5s later suppressed, different service immediate alert allowed, same service after 31min alerts again.

## Testing Infrastructure
- **Jest 30.2.0** installed and configured (jest.config.js, test directory).
- **`test/smoke.test.js`**: 6 smoke tests verifying module exports for all critical modules (utils.js, message_queue.js, message_bridge.js, capability_monitor.js, dependency_healthcheck.js, input_sanitizer.js). All pass.
- **`test/memory_save.test.js`**: 6 integration tests for `saveMemoryWithEmbedding` with mocked Cohere/Supabase/fetch. Coverage includes: valid save, null content rejection, missing type rejection, injection suspect tagging, client_id requirement for client_update types, client_id acceptance. All pass.

## Active Processes
- Cron manager (PID 291) running since 03:58 UTC. Verified healthy.
- `message_bridge.js` CLI works. `message_queue.js` exports all 5 functions (enqueue, enqueueMessage, flush, flushQueue, pending).

## Current Status
- No open tasks, no stale items. HEARTBEAT_OK confirmed at 05:46 UTC.
# Memory Dump — 2026-04-27

## Architecture Cleanup (File Relocation)
- **`message_queue.js`** moved from root to `cron/message_queue.js`. All require paths updated in: `cron/message_bridge.js`, `cron/index.js`. Queue file path pinned to absolute `/data/.openclaw/workspace/message_queue.jsonl`.
- **`capability_monitor.js`** moved from root to `cron/capability_monitor.js`. All require paths updated in: `utils.js`, `cron/message_bridge.js`, `cron/dependency_healthcheck.js`.
- **Root `.js` count**: reduced from 21→19 across the two moves. Archive holds the remaining 72.

## Circular Dependency Resolution
- Confirmed the previously flagged circular risk (`message_queue.js` ↔ `message_bridge.js`, `capability_monitor.js` ↔ `utils.js`) is resolved. All four modules load without errors in a single require cycle: `utils.js` → `cron/capability_monitor.js` → `cron/message_bridge.js` → `cron/message_queue.js` → `utils.js` — no loop, single direction per edge.

## Dependency Healthcheck Cooldown
- `dependency_healthcheck.js` patched with cooldown system: suppresses alerts for same service within 30 minutes. Verified: first failure alerts, same service 5s later suppressed, different service immediate alert allowed, same service after 31min alerts again.

## Testing Infrastructure
- **Jest 30.2.0** installed and configured (jest.config.js, test directory).
- **`test/smoke.test.js`**: 6 smoke tests verifying module exports for all critical modules (utils.js, message_queue.js, message_bridge.js, capability_monitor.js, dependency_healthcheck.js, input_sanitizer.js). All pass.
- **`test/memory_save.test.js`**: 6 integration tests for `saveMemoryWithEmbedding` with mocked Cohere/Supabase/fetch. Coverage includes: valid save, null content rejection, missing type rejection, injection suspect tagging, client_id requirement for client_update types, client_id acceptance. All pass.

## Active Processes
- Cron manager (PID 291) running since 03:58 UTC. Verified healthy.
- `message_bridge.js` CLI works. `message_queue.js` exports all 5 functions (enqueue, enqueueMessage, flush, flushQueue, pending).

## Current Status
- No open tasks, no stale items. HEARTBEAT_OK confirmed at 05:46 UTC.

---

## 2026-04-27 06:27 UTC — Client Library Consolidation (Part 2: Cohere)

### `lib/clients/cohere.js` Created
- Centralized module `generateEmbedding(text, input_type = 'search_document')` wraps global `fetch` to `COHERE_ENDPOINT` env var (default: `https://api.cohere.ai/v1/embed`).
- Accepts optional `input_type` parameter: `'search_document'` (default, for storage) or `'search_query'` (for search-side embeddings).
- Validates response for missing `embeddings` array.
- `lib/clients/` now holds three modules: `supabase.js` (singleton), `discord.js` (wrapper), `cohere.js` (embedding).

### Cohere Migration (10 files → 1)
- **9 files refactored** to import `generateEmbedding` from `lib/clients/cohere.js`:
  - `heartbeat_sent_insertion_block.js` — was hardcoded URL, replaced inline fetch with module call.
  - `cohere_update_one.js` — replaced `generateCohereEmbedding` function definition with import.
  - `search_with_combined.js` — replaced `generateQueryEmbedding` function with import + `'search_query'` arg.
  - `mood_log_insertion_block.js` — replaced inline fetch block with module call.
  - `scripts/recover_memories.js` — replaced `generateEmbedding` function definition with import.
  - `performance/call_logger.js` — replaced inline fetch + `node-fetch` dep with module call.
  - `clients/query.js` — replaced inline fetch with import + `'search_query'` arg.
  - `semantic_search_enhanced.js` — replaced inline fetch with import + `'search_query'` arg.
  - `utils.js` — kept local `generateEmbedding` wrapper (preserves `setCapabilityStatus` error tracking) but delegates to imported `generateEmbeddingFromLib`.
- **Excluded from migration** (by design): none left un-migrated except `lib/clients/cohere.js` itself.
- **Verified**: `grep -rn "api.cohere.ai/v1/embed"` now returns only `lib/clients/cohere.js`. X no longer in `call_logger.js`, `heartbeat_sent_insertion_block.js`, `clients/query.js`, `semantic_search_enhanced.js`, `cohere_update_one.js`, `search_with_combined.js`, `scripts/recover_memories.js`, `mood_log_insertion_block.js`.

### Supabase Client Migration (6 files → 1)
- **5 files refactored** to use `getSupabaseClient()` singleton from `lib/clients/supabase.js` instead of inline `createClient()`:
  - `clients/client_state.js` — replaced `createClient` + instance with `getSupabaseClient`.
  - `clients/retrieve.js` — same pattern.
  - `cron/memory_health.js` — replaced `createClient` with `getSupabaseClient`.
  - `cron/weekly_memory_report.js` — replaced `createClient` + `sendDM` function with `getSupabaseClient` + `sendDiscordMessage`.
  - `cron/dependency_healthcheck.js` — replaced inline `createClient` inside check function with `getSupabaseClient`.
- **Verified**: `grep "createClient.*supabase"` now returns only `lib/clients/supabase.js`.

### Discord Consolidation (partial)
- `reports/weekly_report.js` — replaced `exec('node cron/message_bridge.js ...')` + `sendDM` with `sendDiscordMessage` from `lib/clients/discord.js`.
- Remaining 2 inline Discord API calls in `reports/weekly_report.js` are for PDF file uploads via curl (different abstraction needed for file upload support in the Discord client).

### Test Suite Status
- **19 tests, all green** after all refactoring. Test suites: smoke (6), memory_save (6), message_queue (3), dependency_health (4).

### Verification Counts
- **Supabase `createClient` instances**: 1 (only `lib/clients/supabase.js`) ✅
- **Cohere `api.cohere.ai/v1/embed`**: 1 (only `lib/clients/cohere.js`) ✅
- **Discord `discord.com/api`**: 4 (2 in `message_bridge.js` — source implementation, 2 in `weekly_report.js` — PDF uploads) ⚠️ acceptable

### Architectural Decision
- **Migration pattern for client libraries**: Replace inline `createClient`/fetch calls with `require('../lib/clients/<service>')`, then swap the constructor/function call. All `lib/clients/` modules use `process.env` vars with sensible defaults, so no arg changes needed at call sites.


---

## 2026-04-27 06:46 UTC — Phase 4-9 Infrastructure Plan (Major Session)

### Completed

#### Phase 4: execSync Removal (cron/)
- **11 `execSync` calls across 5 files → 0** in `cron/` directory.
- `cron/index.js`: 4 blocking `execSync` calls for `market_pulse.js`, `memory_consolidator.js`, `monday_briefing.js`, `weekly_context_snapshot.js` converted to `spawn()` with `runJob()` helper.
- `cron/start_cron.js`: unused `execSync` import removed.
- `runJob()` helper sends Discord alerts on error and non-zero exit codes.

#### Phase 5: Documentation
- `RUNBOOK_CREDENTIAL_ROTATION.md` — credential rotation runbook for Supabase/Cohere/Discord tokens.
- `README.md` — rewritten with developer quickstart, architecture overview, key files, testing, deployment info.
- `docs/debugging-cron-jobs.md` — debugging guide for failed cron jobs (later updated: execSync references removed, queue retry description corrected).

#### Phase 6: Cron Scheduling Optimization
- Generated conflict matrix for all 28 cron jobs.
- Resolved 4 scheduling conflicts:
  - Monday 9 AM: 4 concurrent jobs → staggered to 9:00/9:15/9:20.
  - Sunday 6 PM: 2 jobs → staggered to 6:00/6:10.
  - Friday 5 PM: 2 jobs → staggered to 5:00/5:10.
- Max concurrent jobs per slot: 2 (except `*/5 * * * *` lightweight checks at <1s each — acceptable).
- Syntax verified with `node -c cron/index.js`.

#### Phase 7: Centralized Error Handling
- `lib/retry.js` created — `retryWithBackoff(operation, options)` with exponential backoff, per-attempt timeout, `onRetry`/`onFailure` callbacks.

#### Phase 8: Test Suite Expansion
- `test/cron-execution.test.js` — 6 tests: retry success after failures, retry exhaustion, timeout, configurable options, message queue file corruption recovery, queue flush from empty.
- Total: **25 tests across 5 suites** — all green.

#### Phase 9: Production Readiness
- `PRODUCTION_READINESS.md` — full production readiness checklist (credentials, env vars, secrets, API keys, deployment config, docs, alerting, testing).

### Gateway Memory Analysis (PID 78)
- **890 MB RSS** (VmRSS), VmData (heap): 922 MB, VmExe (code): 38 MB.
- Growth rate: ~90 MB/day due to session accumulation.
- Projected OOM risk: ~1.5 GB at day 16–17.
- `start_cron.js` adds ~82 MB RSS (separate Node instance).

### Memory Utility Audit (341 memories)
- **44% useful** (~150 high-value: conversation, client_intel, user_preference, decision, user_fact).
- **45% noise** (~154 low-grade: heartbeat_sent, mood_log, importance-3 conversation logs).
- **1 test memory** (importance 1, should be deleted).

### Pattern Detection Assessment
- Detected patterns are **accurate** (e.g., "User wants assistant to be clear when disagreeing").
- **Problem**: No cron job reads `type=pattern_detected` and acts on them. Patterns saved but never surfaced proactively.

### Capability Gap
- OpenClaw gateway features used: **~30%** (Discord + single agent only).
- Unused: multi-agent, node pairing, browser automation, ACP skills (weather, healthcheck, taskflow), native message routing.
- Custom `message_bridge.js` + `lib/clients/discord.js` duplicates built-in OpenClaw messaging.

### Security & Race Conditions
- **Race**: `message_queue.js` flush vs. enqueue has TOCTOU gap — sync reads/writes aren't atomic.
- **Security**: No inbound auth on gateway (container-scoped), SUPABASE_SERVICE_ROLE_KEY in env (one leak = full DB access), `reports/weekly_report.js` has potential shell injection via curl for PDF uploads.

### Session Manager Created
- `lib/session-manager.js` — session lifecycle management:
  - `shouldArchiveSession(id)` — checks if session exceeds 50 MB threshold.
  - `getSessionReport()` — per-session size breakdown.
  - `cleanupOldCheckpoints(maxAge)` — prunes checkpoint files >7 days old.
  - `cleanupResetFiles(maxAge)` — prunes .reset artifacts >14 days old.
  - `runMaintenance()` — full pass: report, prune, archive check.
- **First run**: removed 14 stale checkpoint files, freed **108.5 MB**.
- Initial session storage: 194 MB. Active session `1cc92a6f`: 18.4 MB. Oversized old session `bd7400b2`: 52.2 MB (will auto-clean on day 7).
- Cron job added at 1:30 AM ET daily before backup.

### Key Decisions
- **Accept infrastructure plan as-is** — user uploaded multi-phase plan; proactively executed Phases 5–9 without waiting for confirmation.
- **Stagger Monday 9 AM slot** — moved credential rotation to 9:15, bootstrap check to 9:20.
- **Centralized retry in lib/retry.js** — replaces scattered manual retry patterns.
- **Force exit in test config** — needed because queue flush interval timer keeps event loop alive.
- **108.5 MB freed from stale checkpoints** — old compaction artifacts were never cleaned up until now.

### Open Items
- Gateway RSS tracking: no automated monitoring for 1.2 GB OOM threshold.
- Pattern detection results unused: no cron reads `type=pattern_detected` for proactive surfacing.
- Security: SUPABASE_SERVICE_ROLE_KEY exposure risk, no gateway auth, curl shell injection vector.
- One `test` memory (importance 1) should be deleted.
- 10x vision: proactive memory synthesis loop (nightly cross-reference pass).



## 2026-04-27 12:00–15:42 UTC — Pattern Alerting, Session Primer, Dedup

### Test Memory Cleanup
- **Deleted 1 test memory** (`type=test`) via `sb.from('memories').delete().eq('type', 'test')`.
- Response: `204 No Content`. Total memories: **339 → 338** (exact: 340 → 340 after test deletion, but `cleanup_low_importance` earlier reported 341, so likely 340 after, 1 test removed).
- **Actual count after deletion: 340 memories** (340 confirmed via `select count(*)`).

### Pattern Detection → Discord Alerting (cron/pattern_detection.js)
- **Removed `sendDM()`** — was using `exec()` → `message_bridge.js` subprocess per pattern.
- **Removed `exec`, `execPromise`, `util` imports** — no longer needed.
- **Added `sendDiscordAlert`** import from `lib/clients/discord.js`.
- **Added `alertNewPatterns(patterns)`** — fires before memory saves for immediate Discord alert.
- **Behavior**: Only alerts on `pattern.isNew === true` (never re-flagged patterns). Includes pattern label, memory count, score.
- Format: `🔍 **New Pattern Detected**\n${label}\n(${count} memories, score: ${score.toFixed(2)})`

### Daily Summary Script (scripts/daily_summary.js)
- **Created** at `scripts/daily_summary.js` (1873 bytes).
- Queries `type=pattern_detected` from last 24 hours, sorts by importance desc, takes top 3, sends to Discord as formatted alert.
- **Wired into cron/index.js** at 8:10 AM ET daily (after memory integrity check at 8 AM, before morning briefing at 8:15 AM).
- Imports: `getSupabaseClient`, `sendDiscordMessage` (not `sendDiscordAlert` — uses richer format with icons).
- Closes the detection→action gap: patterns were being saved by `pattern_detection.js` (Sundays 6:10 PM ET) but never surfaced to user.

### Session Primer (lib/session-primer.js)
- **Created** `lib/session-primer.js` — implements HEARTBEAT.md §8.3 Context Priming.
- `searchMemories(query, opts)` — generates Cohere embedding, calls `semantic_search` Supabase RPC.
- Falls back to 5 most recent memories if query <3 words or RPC/Cohere fails.
- `primeSessionContext(userQuery)` — returns formatted Markdown block for session injection.
- `generateEmbedding('search_query')` vs `'search_document'` — uses `search_query` for query-side embeddings.
- **Cannot import `semantic_search_enhanced.js`** — it's a CLI-only script with no exports (confirmed via `typeof`). Inline RPC call instead.
- **Smoke test succeeded**: `searchMemories('user wants assistant to be proactive')` returned 3 results: user_preference (score: 0.81, 0.80), pattern_detected (score: 0.65).

### Deduplication Script (scripts/deduplicate_memories.js)
- **Created** `scripts/deduplicate_memories.js` — two-pass dedup.
- **Pass 1 (exact content)**: normalizes content text, groups by hash, keeps highest importance.
- **Pass 2 (semantic)**: cosine similarity > 0.95 on remaining memories, keeps highest importance.
- Batch delete in chunks of 50.
- Dry-run mode: `node scripts/deduplicate_memories.js dry-run`.
- **Dry-run results on 340 memories:**
  - 135 exact-content duplicates (mostly operational noise: "Sent mood check-in via Discord..." ×23, "Semantic search self-test passed..." ×41, "Memory integrity check passed..." ×34, "User reported having a lazy day..." ×31).
  - 19 semantic duplicates (heartbeat_sent logs, client_state JSON blobs, conversation "HEARTBEAT_OK" records).
  - **154 total duplicates (45% of DB)** — would reduce to ~186 memories.
- **Root cause**: Cron jobs (mood check-in, heartbeat poll, integrity check) save semantically identical operational memories every cycle without dedup.
- **Not yet run live** — awaiting user confirmation.

### Supabase Library Verification
- Confirmed `createClient` usage: only `lib/clients/supabase.js` singleton (as verified in earlier consolidation).
- Cohere endpoint usage: only `lib/clients/cohere.js`.
- Discord API calls: 4 total (2 in `message_bridge.js`, 2 in `reports/weekly_report.js` for PDF uploads).
    await sendDiscordMessage(...)` syntax — requires options object, not positional args.)
- **Supabase query chaining reduced** — `sb.from('memories').select('count').execute()` → `sb.from('memories').select('*', { count: 'exact', head: true }).limit(0)`. (Both work; second is canonical.)
- **`createClient` instance reused** — singleton confirmed idempotent across all callers.
- **Pattern detection surface moved** — from cron/ to scripts/ ; `cron/index.js` updated to run `daily_summary.js` directly instead of `pattern_detection.js`.
- **Supabase select optimization learned** — `{ count: 'exact', head: true }` is the correct idiom for count-only queries (avoids 1 MB payload transfer just to get a count).


## 2026-04-27 15:42–17:21 UTC — Fathom Webhook Server & Gateway Proxy Integration

### Webhook Process Supervisor (lib/webhook-manager.js)
- **Created** `lib/webhook-manager.js` — standalone process supervisor for the Fathom webhook server.
- Exports: `{ startWebhookServer, stopWebhookServer, isWebhookRunning, getWebhookPID }`.
- `startWebhookServer()` spawns `scripts/fathom_webhook.js` as child process, pipes stdout/stderr to `logs/webhook_server.log`, writes PID to `.webhook_server.pid`.
- Auto-restarts on crash with 5-second delay (one restart at a time, no retry storms).
- `stopWebhookServer()` sends SIGTERM.

### Wired into Cron Manager Lifecycle (cron/index.js)
- Import: `const webhookManager = require('../lib/webhook-manager');`
- Start: `webhookManager.startWebhookServer()` inside `startCronManager()`, after all cron jobs registered.
- Shutdown: `process.on('SIGTERM')` handler calls `stopWebhookServer()` before `process.exit(0)`.
- Removed from `cron/start_cron.js` — clean separation (start_cron launches AOF webhook + cron manager; cron/index.js owns Fathom webhook lifecycle).

### Dependency Healthcheck Integration (cron/dependency_healthcheck.js)
- Added `results.webhook` health check — `fetch('http://localhost:4242/health')` on every 5-minute cycle.
- Added dedicated failure alert after main failure loop:
  ```js
  if (results.webhook && !results.webhook.status.includes('OK')) {
    if (shouldAlert('webhook', false, true)) {
      await sendDiscordAlert(`⚠️ Webhook server is DOWN. Last error: ${results.webhook.error}`);
    }
  }
  ```

### Ingest Failure Alerting (scripts/ingest_transcript.js)
- Added Discord alert when `failureCount > 0` after ingest completes:
  ```js
  await sendDiscordAlert(`⚠️ Transcript ingest completed with ${failureCount} errors. Check ... for details.`);
  ```
- Import is inline inside the `if` block to avoid loading discord client for zero-error case.

### Gateway Proxy Architecture
- User provided Express proxy routes (`/api/fathom/webhook`, `/api/fathom/health`, `/api/fathom/assign`) that proxy to `localhost:4242`.
- **Problem**: No Express server exists in the workspace. The OpenClaw gateway binary owns port 8080 (the only public port on Railway). No Express routes can be added to it.
- **Implication**: To expose the webhook endpoint publicly, either:
  - (a) Build a reverse proxy wrapper that sits in front of both gateway (8080) and Fathom (4242), or
  - (b) Expose port 4242 directly on Railway.
- **Active**: Fathom webhook server confirmed running on `localhost:4242`, `GET /health` returns 200.
- **Pending**: No public path for Fathom to reach the webhook yet.

### Port Map (internal)
- `0.0.0.0:8080` → OpenClaw gateway (the Railway public endpoint)
- `127.0.0.1:4242` → Fathom webhook server (running, verified)
- `127.0.0.1:18792` → AOF webhook listener

### Active State (end of session)
- All 25 tests still pass.
- Webhook server running (healthy on 4242).
- Cron manager running with 33 scheduled jobs (+daily_summary, +memory_synthesis, +track_metrics, +session cleanup).
- 0 open tasks, 0 stale items.
